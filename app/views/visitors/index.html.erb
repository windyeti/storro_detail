<% if !user_signed_in? %>
<h3>Welcome</h3>
<p>This is our app</p>
<ul class="form-sign-help">
<%= render "layouts/nav_links_for_auth" %>
</ul>
<% end %>
<% if user_signed_in? %>
<p>Всего товаров: <%=Product.product_all_size %></p>
<p>Больше 0: <%= Product.product_qt_not_null_size %></p>
<% file = "#{Rails.public_path}"+'/ins_c44kz.csv'%>
<% if File.file?(file) %>
<% size = File.size(file) %>
<p>Файл инсалес:
<p>размер - <%= number_to_human_size(size)%>; дата - <%=File.ctime(file).in_time_zone('Moscow').strftime("%d/%m/%Y %H:%M" ) %></p>
<p><a href="/ins_c44kz.csv">Скачать файл</a> </p>
<%end%>

<% if Rails.cache.read('file_qt_update') != nil %>
<p>Остатки обновлялись в - <%= File.ctime(Rails.cache.read('file_qt_update')).in_time_zone('Moscow').strftime("%d/%m/%Y %H:%M" ) %></p>
<% end %>

<% end %>
